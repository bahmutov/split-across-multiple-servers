name: ci
on: push
jobs:
  tests:
    # run 3 copies of the current job in parallel
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        containers: [1, 2, 3]
    steps:
      - name: Checkout 🛎
        uses: actions/checkout@v4

      - name: Run split Cypress tests 🧪
        uses: cypress-io/github-action@v6
        with:
          # need to start a server on each machine
          # on machine 0 use the start0 script
          # on machine 1 use the start1 script
          # on machine 2 use the start2 script
          start: npm run start0
        # TODO: pass the environment variables
        # to the cypress-split plugin
        # https://github.com/bahmutov/cypress-split
